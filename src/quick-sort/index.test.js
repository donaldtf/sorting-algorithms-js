import test from 'ava';
import { quickSort } from './';
import { testCases } from '../test-cases';

// testCases.forEach((testCase) => {
// 	test(`quick sort - ${testCase.name}`, t => {
// 		t.deepEqual(quickSort(testCase.input), testCase.expected);
// 	});
// })
