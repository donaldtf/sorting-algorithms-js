import test from 'ava';
import { heapSort } from './';
import { testCases } from '../test-cases';

// testCases.forEach((testCase) => {
// 	test(`heap sort - ${testCase.name}`, t => {
// 		t.deepEqual(heapSort(testCase.input), testCase.expected);
// 	});
// })
