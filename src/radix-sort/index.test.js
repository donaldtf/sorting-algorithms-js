import test from 'ava';
import { radixSort } from './';
import { testCases } from '../test-cases';

// testCases.forEach((testCase) => {
// 	test(`radix sort - ${testCase.name}`, t => {
// 		t.deepEqual(radixSort(testCase.input), testCase.expected);
// 	});
// })
